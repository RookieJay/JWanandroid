<?xml version="1.0" encoding="utf-8"?>
<resources>
    
    <!--提示条-->
    <declare-styleable name="LabelView">
        <!-- 设置文字内容 -->
        <attr name="lv_text" format="string"/>
        <!-- 设置文字颜色,默认#ffffff -->
        <attr name="lv_text_color" format="color"/>
        <!-- 设置文字大小,默认11sp -->
        <attr name="lv_text_size" format="dimension"/>
        <!-- 设置文字是否支持加粗,默认true -->
        <attr name="lv_text_bold" format="boolean"/>
        <!-- 设置文字是否支持全部大写,默认true-->
        <attr name="lv_text_all_caps" format="boolean"/>
        <!-- 设置背景颜色,默认"#FF4081"-->
        <attr name="lv_background_color" format="color"/>
        <!-- 设置LabelView所在矩形最小宽高,默认mFillTriangle?35dp:50dp-->
        <attr name="lv_min_size" format="dimension"/>
        <!-- 设置文字上下padding,默认3.5dp,mFillTriangle为true时无效-->
        <attr name="lv_padding" format="dimension"/>
        <!-- 设置LabelView方向 -->
        <attr name="lv_gravity" format="enum">
            <enum name="TOP_LEFT" value="51"/>
            <enum name="TOP_RIGHT" value="53"/>
            <enum name="BOTTOM_LEFT" value="83"/>
            <enum name="BOTTOM_RIGHT" value="85"/>
        </attr>
        <!-- 设置是否填充三角区域,默认false -->
        <attr name="lv_fill_triangle" format="boolean"/>
    
    </declare-styleable>
    
    <!-- 自定义圆圈进度条 -->
    <declare-styleable name="CircleProgress">
        <!-- 填充实心颜色 -->
        <attr name="cp_solid_color" format="color"/>
        <!-- 边框颜色 -->
        <attr name="cp_stroke_color" format="color"/>
        <!-- 填充颜色 -->
        <attr name="cp_stroke_width" format="dimension"/>
        <!-- 进度条颜色 -->
        <attr name="cp_progress_color" format="color"/>
        <!-- 进度条宽度 -->
        <attr name="cp_progress_width" format="dimension"/>
        <!-- 圆半径 -->
        <attr name="cp_circle_radius" format="dimension"/>
    </declare-styleable>
    
    <declare-styleable name="WebViewProgress">
        <!-- 进度 -->
        <attr name="progress" format="integer"/>
        <!-- 最大值 -->
        <attr name="max" format="integer"/>
        <!-- 进度条颜色 -->
        <attr name="progress_color" format="color"/>
    </declare-styleable>
    
    <!-- 今日诗词控件  -->
    <declare-styleable name="JinrishiciTextView">
        <attr format="boolean" name="jrsc_refresh_on_click"/>
        <attr format="boolean" name="jrsc_show_error"/>
        <attr format="boolean" name="jrsc_show_loading_text"/>
        <attr format="string" name="jrsc_text_loading"/>
        <attr format="string" name="jrsc_text_error"/>
    </declare-styleable>
    
</resources>